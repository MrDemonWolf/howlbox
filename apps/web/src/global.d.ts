// Build-time constants injected by the define block in vite.config.ts.
// Full commit sha in CI and local git builds, the literal "dev" when
// neither is available.
declare const __COMMIT_HASH__: string;
